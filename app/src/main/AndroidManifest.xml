<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="polinema.ac.id.dtsdesigntolayout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.RegisterResultActivity"></activity>
        <activity android:name=".activity.SuccessActivity" />
        <activity android:name=".activity.ResetPassword" />
        <activity android:name=".activity.ForgotPassword" />
        <activity android:name=".activity.WelcomeBack" />
        <activity android:name=".activity.WelcomeSlideAssign" />
        <activity android:name=".activity.WelcomeSlideSuperhero" />
        <activity android:name=".activity.WelcomeSlideCalendar" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>